services:
  vlegal-backend:
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: vlegal-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
